/**
 * みっけ！ シェル設定
 * デプロイ後にこの 3 つを埋めてください（README「導入方法」参照）
 */
window.APP_CONFIG = {
  // デプロイT（教員ポータル / 実行するユーザー: ウェブアプリケーションにアクセスしているユーザー）の exec URL
  execUrlT: "",
  // デプロイS（児童用 / 実行するユーザー: 自分＝アプリアカウント）の exec URL
  execUrlS: "",
  // GIS 用 OAuth クライアントID（ウェブアプリ種別）。GAS 側 ScriptProperties の sp_googleClientId と同じ値
  googleClientId: ""
};
